[user]
	email = f0i.git@projects.f0i.de
	name = f0i
	signingkey = 19810A7F
[core]
	excludesfile = ~/.gitignore
[merge]
	tool = vimdiff
[commit]
  gpgsign = true
[color]
	status = auto
	diff = auto
	branch = auto
  ui = auto
  interactive = auto
[push]
	default = simple
	followTags = true
[github]
	user = f0i
[log]
	decorate = short
[alias]
  s = status
  p = diff
  b = branch
  l = log --graph --pretty=format:'%C(yellow)%h%C(cyan)%d %C(green)%an %C(cyan)%G?%Creset %s %C(green)%ad - %ar%Creset' --date=short
	la = !git l --all
  lf = !git l --stat
  laf = !git la --stat
  lfa = !git laf
  lp = !git l --patch
  lp1 = !git lp -1
  lp2 = !git lp -2
  lwp = !git lp --color-words
  lap = !git la --patch
  lawp = !git lap --color-word
  wp = diff --color-words
  wdiff = diff --color-words
  ls = ls-files
  c = commit -v
  ca = commit -av
  stas = -c commit.gpgsign=false stash
  git = !git
